(program
  (import TestBase)

  (structure Click)
  (external (: click Click))

  (structure name (: x Int) (: y Bool))

  (: std_test (List Int))
  (= std_test (initnext (list 0 1) (push! std_test 2)))  
)